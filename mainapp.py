from common.utils import *


if __name__ == '__main__':
    # funds = 100
    test_percent_array = [1, 2, 3, 4]
    for percent in test_percent_array:
        current_percent = percent_calculation(percent)
        print(current_percent)
